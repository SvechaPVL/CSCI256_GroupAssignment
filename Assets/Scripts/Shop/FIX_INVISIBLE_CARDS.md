# 🔧 Исправление невидимых карточек предметов

## Проблема
ItemCard префаб прозрачный или почти невидимый в магазине.

## ✅ Решение (30 секунд!)

### Шаг 1: Удалите старый префаб
```
1. Assets/Prefabs/Shop/ItemCardPrefab.prefab
2. ПКМ → Delete (или нажмите Delete)
3. Подтвердите удаление
```

### Шаг 2: Создайте НОВЫЙ видимый префаб
```
1. Unity → Tools → Shop System → Create Shop UI
2. Нажмите: "Create Only Item Card Prefab"
3. Увидите сообщение: "🌟 СУПЕР-ВИДИМЫЙ Item Card Prefab создан! 🌟"
```

### Шаг 3: Переназначьте префаб в ShopManager
```
1. Hierarchy → ShopManager → Inspector
2. Item Card Prefab → перетащите новый ItemCardPrefab из Assets/Prefabs/Shop/
3. Готово!
```

### Шаг 4: Запустите игру и проверьте
```
1. Нажмите Play
2. Откройте магазин (E)
3. Теперь карточки ОЧЕНЬ видимые:
   - Тёмно-синий фон
   - Золотая обводка
   - Яркий белый текст
   - Золотая цена
   - Видимый placeholder для иконки
```

---

## 🎨 Что изменилось?

### БЫЛО (старая версия):
- ❌ Тёмно-серый фон (почти не видно)
- ❌ Тусклая обводка
- ❌ Прозрачный background (alpha: 0.05)
- ❌ Тёмный placeholder для иконки

### СТАЛО (новая версия):
- ✅ **Тёмно-синий фон** (RGB: 51, 77, 102) - полностью непрозрачный!
- ✅ **ЗОЛОТАЯ обводка** (RGB: 255, 217, 0) - 3px толщина
- ✅ **ЯРКИЙ белый текст** с чёрной тенью для читаемости
- ✅ **ЯРКО-золотая цена** (RGB: 255, 230, 51) с тенью
- ✅ **Видимый серый placeholder** для иконки (RGB: 204, 204, 204)
- ✅ Placeholder текст: "ITEM NAME" и "50 Scrap" (без эмодзи для совместимости)
- ✅ Hover эффект: карточка светлеет при наведении

---

## 📊 Технические детали изменений

### Корневой Image (фон карточки):
```csharp
БЫЛО:  new Color(0.25f, 0.25f, 0.25f, 1f)  // Тёмно-серый
СТАЛО: new Color(0.2f, 0.3f, 0.4f, 1f)     // Тёмно-синий
```

### Outline (обводка):
```csharp
БЫЛО:  new Color(0.5f, 0.5f, 0.5f, 0.8f)   // Серая
       effectDistance = (2, -2)
СТАЛО: new Color(1f, 0.85f, 0f, 1f)        // ЗОЛОТАЯ
       effectDistance = (3, -3)             // Толще!
```

### Icon placeholder:
```csharp
БЫЛО:  new Color(0.6f, 0.6f, 0.6f, 0.5f)   // Полупрозрачный
СТАЛО: new Color(0.8f, 0.8f, 0.8f, 1f)     // Полностью непрозрачный
```

### Текст (Name):
```csharp
БЫЛО:  fontSize = 22, color = White
СТАЛО: fontSize = 24, color = White + Shadow (2, -2)
```

### Цена (Price):
```csharp
БЫЛО:  fontSize = 18, color = (1, 0.85, 0)
СТАЛО: fontSize = 20, color = (1, 0.9, 0.2) + Shadow (2, -2)
```

---

## 🐛 Если всё ещё не видно

### Проблема 1: Карточки вообще не появляются
**Решение:**
```
1. ShopManager → Available Items
2. Убедитесь что там есть ScriptableObject'ы предметов
3. Если пусто - добавьте предметы из Assets/ScriptableObjects/ShopItems/
```

### Проблема 2: Карточки есть, но текст не виден
**Решение:**
```
1. Откройте ItemCardPrefab
2. Проверьте TextMeshProUGUI компоненты:
   - NameText: Color должен быть White (255, 255, 255, 255)
   - PriceText: Color должен быть Gold (255, 230, 51, 255)
   - StockText: Color должен быть Light Gray (230, 230, 230, 255)
```

### Проблема 3: Фон карточки всё равно прозрачный
**Решение:**
```
1. Откройте ItemCardPrefab
2. Выберите корневой объект (ItemCard)
3. Image Component → Color
4. Проверьте Alpha = 255 (или 1.0 в float)
5. RGB: (51, 77, 102) - тёмно-синий
```

### Проблема 4: Обводки нет
**Решение:**
```
1. Откройте ItemCardPrefab
2. Корневой объект → Add Component → Outline
3. Effect Color: RGB(255, 217, 0, 255) - золотой
4. Effect Distance: X=3, Y=-3
```

---

## 😊 Хотите добавить эмодзи?

Эмодзи **убраны из кода** для совместимости с TextMeshPro шрифтом по умолчанию.

Если вы хотите добавить эмодзи (💰, 🗡️ и т.д.), см. **[EMOJI_SUPPORT.md](EMOJI_SUPPORT.md)** - полная инструкция!

---

## 💡 Дополнительная настройка

### Хотите другой цвет фона?

Откройте `Assets/Scripts/Shop/Editor/ShopUICreator.cs` и измените строку:

```csharp
// Строка ~453
cardImage.color = new Color(0.2f, 0.3f, 0.4f, 1f); // Измените эти значения

// Примеры:
// Тёмно-зелёный: new Color(0.2f, 0.4f, 0.3f, 1f)
// Тёмно-красный:  new Color(0.4f, 0.2f, 0.2f, 1f)
// Тёмно-фиолетовый: new Color(0.3f, 0.2f, 0.4f, 1f)
// Чёрный: new Color(0.1f, 0.1f, 0.1f, 1f)
```

После изменения пересоздайте префаб (Шаги 1-2 выше).

### Хотите изменить цвет обводки?

```csharp
// Строка ~467
outline.effectColor = new Color(1f, 0.85f, 0f, 1f); // Золотой

// Примеры:
// Серебряный: new Color(0.8f, 0.8f, 0.9f, 1f)
// Красный: new Color(1f, 0.2f, 0.2f, 1f)
// Синий: new Color(0.2f, 0.5f, 1f, 1f)
// Зелёный: new Color(0.2f, 1f, 0.2f, 1f)
```

---

## 🎯 Проверочный чеклист

После пересоздания префаба проверьте:

- [ ] Карточки видны в магазине
- [ ] Фон карточек тёмно-синий (не прозрачный)
- [ ] Вокруг карточек золотая обводка
- [ ] Текст названия белый и читаемый
- [ ] Цена золотая и читаемая
- [ ] Иконка-placeholder видна (серый квадрат)
- [ ] При наведении карточка становится светлее
- [ ] Тени под текстом видны

Если все пункты ✅ - всё работает отлично!

---

**Готово! Карточки теперь СУПЕР-ВИДИМЫЕ! 🎮✨**
