# 🚨 БЫСТРОЕ ИСПРАВЛЕНИЕ: Карточки есть, но не видны

## Ваша ситуация:
- ✅ Логи показывают что карточки создаются
- ✅ Клик по карточкам работает (детальная панель открывается)
- ❌ Карточки визуально НЕ ВИДНЫ в ScrollView

---

## 🔍 ШАГ 1: Соберите диагностику (1 минута)

### Запустите игру и проверьте Console:

1. **Запустите игру** (Play)
2. **Откройте магазин** (E)
3. **Посмотрите в Console** - найдите новые логи:

Вы должны увидеть:
```
[ShopManager] Card RectTransform - Size: (X, Y), Position: (...), Scale: (...)
[ShopManager] Card Image - Color: RGBA(...), Enabled: True/False
```

### Проверьте эти значения:

#### ❌ Если Size = (0, 0)
**ПРОБЛЕМА:** Карточки имеют нулевой размер!

**РЕШЕНИЕ:**
```
1. Откройте префаб: Assets/Prefabs/Shop/ItemCardPrefab.prefab
2. Выберите корневой объект
3. RectTransform → Width: 400, Height: 100
4. Сохраните (Ctrl+S)
5. Перезапустите игру
```

#### ❌ Если Scale = (0, 0, 0)
**ПРОБЛЕМА:** Карточки имеют нулевой scale!

**РЕШЕНИЕ:**
```
1. Откройте префаб: Assets/Prefabs/Shop/ItemCardPrefab.prefab
2. Корневой объект → Scale: (1, 1, 1)
3. Сохраните (Ctrl+S)
```

#### ❌ Если Color Alpha = 0
**ПРОБЛЕМА:** Карточки полностью прозрачные!

**РЕШЕНИЕ:**
```
1. Откройте префаб
2. Image Component → Color → A (Alpha): 255
3. Убедитесь RGB = (51, 77, 102) для тёмно-синего
4. Сохраните
```

#### ❌ Если Position очень большая (например, 10000)
**ПРОБЛЕМА:** Карточки за пределами экрана!

**РЕШЕНИЕ:**
```
Это баг Layout. См. Шаг 3.
```

---

## 🔍 ШАГ 2: Используйте DebugCardVisibility (2 минуты)

### Добавьте debug компонент:

1. **Запустите игру** (Play)
2. **В Hierarchy** найдите: `Canvas/ShopPanel/ItemScrollView/Viewport/Content`
3. **Add Component** → `DebugCardVisibility`
4. **Нажмите F1** в игре
5. **Смотрите Console** - появится детальная информация о всех карточках

### Что искать:

**В Scene View:**
- Должны быть видны жёлтые рамки вокруг карточек
- Красная рамка вокруг Content
- Если рамок нет - размеры = 0!

**В Console:**
- Child count должен быть > 0
- Size каждого ребёнка должен быть (400, 100)
- Position должна меняться: (0, 0), (0, -110), (0, -220)...
- Image Color Alpha должен быть ~1.0

---

## 🔧 ШАГ 3: Быстрые исправления

### Исправление A: Пересоздайте префаб

**Самое надёжное решение:**

```
1. Удалите: Assets/Prefabs/Shop/ItemCardPrefab.prefab
2. Unity → Tools → Shop System → Create Shop UI
3. Нажмите: "Create Only Item Card Prefab"
4. ShopManager → Item Card Prefab → назначьте новый префаб
5. Запустите игру
```

### Исправление B: Проверьте ScrollView настройки

**В Hierarchy:**
```
Canvas/ShopPanel/ItemScrollView
```

**RectTransform должен быть:**
```
Anchor Min: (0, 0)
Anchor Max: (0.6, 1)
Anchored Position: (50, -50)
Size Delta: (-100, -150)
```

**Viewport:**
```
Canvas/ShopPanel/ItemScrollView/Viewport
```

**RectTransform:**
```
Anchor Min: (0, 0)
Anchor Max: (1, 1)
Size Delta: (0, 0)
```

**Mask Component:**
- ✓ Должен быть включен
- Show Mask Graphic: ✗ (выключено)

**Content:**
```
Canvas/ShopPanel/ItemScrollView/Viewport/Content
```

**RectTransform:**
```
Anchor Min: (0, 1)
Anchor Max: (1, 1)
Pivot: (0.5, 1)
Anchored Position: (0, 0)
```

**VerticalLayoutGroup:**
```
Spacing: 10
Padding: 10, 10, 10, 10
Child Control Width: ✓
Child Control Height: ✗ (ВАЖНО!)
Child Force Expand Width: ✓
Child Force Expand Height: ✗ (ВАЖНО!)
```

**ContentSizeFitter:**
```
Horizontal Fit: Unconstrained
Vertical Fit: Preferred Size (ВАЖНО!)
```

### Исправление C: Временно отключите Mask

Чтобы проверить, не Mask ли виноват:

```
1. Запустите игру
2. Hierarchy → Canvas/ShopPanel/ItemScrollView/Viewport
3. Inspector → Mask Component → снимите галочку (disable)
4. Видны ли карточки теперь?
```

**Если ДА** - проблема в размере Viewport или позиции карточек
**Если НЕТ** - проблема в самих карточках (размер, цвет, alpha)

---

## 🎯 ШАГ 4: Проверка в Scene View

### Визуальная диагностика:

1. **Запустите игру** (Play)
2. **Откройте магазин** (E)
3. **Переключитесь на Scene View** (вкладка вверху)
4. **В Hierarchy выберите Content**
5. **Нажмите F** (Focus) - камера направится на Content
6. **Видите ли вы карточки в Scene View?**

### Результат:

**Если видите карточки в Scene View, но не в Game View:**
- Проблема с Canvas/Camera настройками
- Или карточки за пределами Viewport
- Проверьте Mask настройки

**Если НЕ видите карточки даже в Scene View:**
- Карточки действительно невидимы (alpha = 0, size = 0, или scale = 0)
- Пересоздайте префаб (Исправление A)

---

## 🔥 РАДИКАЛЬНОЕ РЕШЕНИЕ: Пересоздать всё

Если ничего не помогло:

```
1. Hierarchy → удалите Canvas полностью
2. Unity → Tools → Shop System → Create Shop UI
3. "CREATE COMPLETE SHOP UI"
4. Создайте предметы: Tools → Shop System → Create Shop Items → "CREATE ALL ITEMS"
5. Hierarchy → создайте пустой GameObject → "ShopManager"
6. Add Component → ShopManager
7. Назначьте все ссылки:
   - Available Items: предметы из Assets/ScriptableObjects/ShopItems/
   - Shop UI: Canvas/ShopPanel
   - Prompt UI: Canvas/PromptPanel
   - Item Cards Container: Canvas/ShopPanel/ItemScrollView/Viewport/Content
   - Item Card Prefab: Assets/Prefabs/Shop/ItemCardPrefab
   - Detail Panel: Canvas/ShopPanel/DetailPanel
   - И остальные ссылки из DetailPanel
8. Запустите игру
```

---

## 📊 Контрольный чеклист

После каждого исправления проверьте:

**Префаб:**
- [ ] Width: 400, Height: 100
- [ ] Scale: (1, 1, 1)
- [ ] Image Color Alpha: 255 (не 0!)
- [ ] Image Color RGB: (51, 77, 102)
- [ ] LayoutElement: Preferred Height = 100

**Content:**
- [ ] VerticalLayoutGroup присутствует
- [ ] ContentSizeFitter → Vertical Fit = Preferred Size
- [ ] Child Control Height = OFF
- [ ] Child Force Expand Height = OFF

**ScrollView/Viewport:**
- [ ] Mask компонент включен
- [ ] Viewport RectTransform anchors правильные
- [ ] ScrollView RectTransform имеет видимую область

**Логи:**
- [ ] "Card created" для каждого предмета
- [ ] Size > 0
- [ ] Scale = (1, 1, 1)
- [ ] Color Alpha > 0

---

## 🆘 Если всё ещё не работает

**Пришлите мне:**

1. Скриншот Console с логами от `[ShopManager]`
2. Скриншот Inspector префаба ItemCardPrefab (корневой объект)
3. Скриншот Inspector Content объекта
4. Скриншот Scene View (когда магазин открыт)

**Укажите:**
- Size из лога: `(?, ?)`
- Position из лога: `(?, ?)`
- Scale из лога: `(?, ?, ?)`
- Color Alpha из лога: `?`

И я точно найду проблему! 🔧
